# Information about this folder
This folder contains the output produced by Ultralytics after completing model training. 

Results for each of the three models (v10, v11, v12) on different patch sizes (320, 640, 1280) can be found along with the best model weights (.pt & .torchscript) along with the model weights from the last epoch
